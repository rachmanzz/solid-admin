# Base URL for the REST API. Defaults to the testing API when unset.
# Override this for a different environment (e.g. a local or production API).
VITE_API_BASE_URL=https://api-testing.rahman-tech.my.id
